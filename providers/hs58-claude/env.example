# Anthropic API Key
ANTHROPIC_API_KEY=sk-ant-api03-...

# Server Configuration
PORT=3000
HOST=0.0.0.0

# DRAIN Configuration
CHAIN_ID=137
# CHAIN_ID=80002  # Use this for Amoy testnet

# Provider Wallet (for claiming payments)
# This wallet receives DRAIN payments
PROVIDER_PRIVATE_KEY=0x...

# Claiming Threshold (claim when earned >= this amount, in USDC wei)
# Default: $10 = 10000000
CLAIM_THRESHOLD=10000000

# Storage (for vouchers)
STORAGE_PATH=./data/vouchers.json
